#include "holberton.h"
/**
 *str_size - function that calculate the length of a string
 *@str: char pointer argument
 *Return: integer
 */
int str_size(char *str)
{
	int aux = 0;

	for (; str[aux] != '\0'; aux++)
	{
	}
	return (aux);
}
